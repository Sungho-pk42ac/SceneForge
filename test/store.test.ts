import { describe, expect, it, beforeEach } from 'vitest';
import { useEditorStore } from '@/store';
import { createDemoScene } from '@/scene';

describe('editor reducer/history',()=>{ beforeEach(()=>{ useEditorStore.setState({document:createDemoScene(),selectedId:null,history:{past:[],future:[]},saveStatus:'saved'} as any); }); it('undo/redo restores transform edits',()=>{ const id=Object.values(useEditorStore.getState().document.nodes).find(n=>n.name==='Hero Cube')!.id; useEditorStore.getState().select(id); useEditorStore.getState().updateTransform(id,'position',{x:2,y:3,z:4}); expect(useEditorStore.getState().document.nodes[id].position.x).toBe(2); useEditorStore.getState().undo(); expect(useEditorStore.getState().document.nodes[id].position.x).not.toBe(2); useEditorStore.getState().redo(); expect(useEditorStore.getState().document.nodes[id].position.x).toBe(2); }); it('undo/redo restores material and hierarchy operations',()=>{ const id=Object.values(useEditorStore.getState().document.nodes).find(n=>n.name==='Hero Cube')!.id; useEditorStore.getState().select(id); useEditorStore.getState().updateMaterial(id,{baseColor:'#123456'}); expect(Object.values(useEditorStore.getState().document.materials).some(m=>m.baseColor==='#123456')).toBe(true); useEditorStore.getState().undo(); expect(Object.values(useEditorStore.getState().document.materials).some(m=>m.baseColor==='#123456')).toBe(false); useEditorStore.getState().redo(); useEditorStore.getState().groupSelected(); const groupId=useEditorStore.getState().selectedId!; expect(useEditorStore.getState().document.nodes[id].parentId).toBe(groupId); useEditorStore.getState().undo(); expect(useEditorStore.getState().document.nodes[id].parentId).toBeNull(); }); it('defends against NaN and zero scale',()=>{ const id=Object.keys(useEditorStore.getState().document.nodes)[0]; useEditorStore.getState().updateTransform(id,'scale',{x:0,y:Number.NaN,z:2}); const scale=useEditorStore.getState().document.nodes[id].scale; expect(scale.x).toBeGreaterThan(0); expect(scale.y).toBe(1); }); });
